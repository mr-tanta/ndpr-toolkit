/**
 * Adapters module — pluggable storage backends for all hooks.
 * No React dependency. Works in any JavaScript/TypeScript environment.
 *
 * Usage: import { localStorageAdapter, apiAdapter } from '@tantainnovative/ndpr-toolkit/adapters'
 */
export * from './adapters/index';
