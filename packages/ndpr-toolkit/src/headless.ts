/**
 * Headless module — alias for `/hooks`.
 * Same exports as `@tantainnovative/ndpr-toolkit/hooks`, surfaced under the
 * more discoverable "headless" name (no UI, just React hooks).
 *
 * Usage: import { useConsent, useLawfulBasis } from '@tantainnovative/ndpr-toolkit/headless'
 */
export * from './hooks-entry';
