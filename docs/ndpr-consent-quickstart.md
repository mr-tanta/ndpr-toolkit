# NDPRConsent quickstart GIF

Binary GIF pending upload via browser GitHub UI (MCP `create_or_update_file` / `push_files` accept text content only; ~278KB GIF cannot be sent as tool-call text).

Target path once uploaded: [`docs/ndpr-consent-quickstart.gif`](./ndpr-consent-quickstart.gif)

Local file ready on agent box: `/workspace/ndpr-star-growth/docs/ndpr-consent-quickstart.gif` (284382 bytes).
